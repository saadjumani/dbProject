<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.dbproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupPage" />
        <activity android:name=".NewsFeedPage" />
        <activity android:name=".TweetPage" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".FollowPage" />

        <provider
            android:name=".data.TweetProvider"
            android:authorities="com.example.hp.dbproject"
            android:exported="false" />

        <activity android:name=".ShowTweet" />
        <activity android:name=".ShowProfileDetailed" />
        <activity android:name=".DisplayMessages" />
        <activity android:name=".WriteMessage" />
        <activity android:name=".MessageDetails"></activity>
    </application>

</manifest>